# Keystatic storage mode. Leave unset for GitHub storage (production),
# set to "local" for local filesystem dev without GitHub auth.
KEYSTATIC_STORAGE_KIND=local

# GitHub repo for Keystatic when storage kind is not "local".
# Format: owner/repo
KEYSTATIC_GITHUB_REPO=DevonTallMan/Signal
